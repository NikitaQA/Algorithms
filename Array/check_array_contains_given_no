public boolean check(int arr[], int num){
    for(int i=0;i<arr.length;i++){
      if(num == arr[i]){
        SOP("Number found at position: " + i);
        return true;
      }
    }
     return false;
}
