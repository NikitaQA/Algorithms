public void findPairs(int arr[], int sum){
  for(int i=0;i<arr.length;i++){
    int first = arr[i];
    for(int j=1;j<arr.length;j++){
      int second = arr[j];
      if(arr[i]+arr[j] == sum){
        SOP("Pair: " + arr[i] + "," + arr[j]);
      }
    }    
  }
}
